Day 3 - Agent Skills.

Massive encyclopedia - gets LLM dumber.

More context not equal more capability.

Attention Dillution.

light-weight primitve : AGENT SKILL

Avoid Context Rot.

PORTABLE AGENT SKILLS.

Directories around central SKILL.md file.

.agents/some-skill/SKILL.md and other files.

flex into hundreds of specialist roles efficeiently.

NATURAL LANGUAGE PROMPTS.

METADATA : name, when to use.

scripts, references, assets.

SEPARATE WHAT TO DO from how to do determinism.

references - lots of TXT.

assets - templates.

PATH A - non technical
PATH B - technical (skill folder)

"PROGRESSIVE DISCLOSURE" - only visible when necessary.

No giant md files.

AGENT.md - global, always in CONTEXT - skills are on demand.

_________________________________________

context rot: magically solve all problems.

dumping everything in.

Lost in the middle study: prove - performance degrades silently as context grows - no error just dumber.

"progressive disclosure" - triggers tools into context. keeps noise out. only load metadata for needed workflows.

RAG pipeline.

Progressive swap tools in out single agent - what about multi agent architecture?

Multi agent reduced to security posture, swarm approach dying. 

operational nightmare to maintain  mutli-agent.


_______________________________________________________________

STUDY: skills get ignored.

1) trigger failuer (prompt vague)
2) execution failure (instructions messy - hallucinated)
3) token budget failure (massive unoptimized pdf)
4) regression (new skill but calls old trigger in routing - break system with added skill)

EDD - to avoid regression.

Evaluation driven Development.

Input, tools, output, defined up front.

Testing skill in isolation.

Run JSON evals (evils)

then "TRAJECTORY SCORING" - exact, in order, out order.

Score TRAJECTORY is better than ANSWER MEASURE.

The ORDER of TOOL CALLS is evaluated NOT the ANSWER.

Trajectory scoring - exact sequence of actions (in order scoring).

SANDBOX - demo to deploy app. shatters in production.

raarely model problem - environment fit problem.

__________________________________________________

STUDY: claude 98.4& operational infrastructure - 
:permission
retry
compaction

1.5% prompt reasoning loop.

foundation model is commodity - guard rails are key.

LLM is not Asset. The harness it the asset.

_________________________________________________

Sandbox

1) read only : LLM as Judge
2) draft only : HITL
3) action allowed : pass to the K

Pass to the K - sustained reliability.

PATH B Developers.

_____________________________________________________

META-SKILLS

authoring
assisted authoring
optimize skill
library evolution.

notices problem and makes adjustments.

evolve own operating optimizations.

pristine evaluation suite.

non negotiable - must add every change to LIBRARY at DRAFT tier.

HITL - for the first few edits.

___________________________________________________________

ORCHESTRATION CHALLENGE.

context rot - cannot use CONTEXT as DATABASE.

DAG - directed acyclic graph.

DAG ORCHESTRATION.

acyclic - no cycles - no loops -map of dependencies.

capability profiles over file message bus.

ONLY PASS URL of FILE.

SHIFT INTELLIGENCE LEFT.

RELY on constraints not engineering.

script tools. 

_____________________________________________

https://github.com/google/skills


_______________________________________________

prmitive : scripts, ref, ass.

call scripts on demand.

multi-agent setups (hard to maintain and trace).

trigger failures, output errors. regression errors.

______________________________________________________

App Store for SKILLS. 

secured tested no malicious injections.

future models optimize metaskills.

debanshu das

pattern of dag orchestration.

________________________________________________

model trained with skills, and tools.

move prompt to FILE BUS.

PASS REFERENCE URL not the VALUES.

PASS POINTERS of REFERENCE.

PROTECT MODEL ATTENTION.

CONTEXT STAYS SMALL.

ONLY SEES REASONING of PRESERVED CONTEXT.

SYSTEM BECOMES DEBUGGABLE. - off of the DISK.

CONTEXT WINDOW not a DB.

SKILLS is DYNAMIC LOADING of tools, resources, assets.

PASS POINTERS.

______________________________________________________________


multiple skils can be used.

which one?

routing llm to skill - 

skill managements. 

routing skill managements.

________________________________________________________________

heirarchical routing library

context rot is distractors / noise

routing breaks overload. 

SEMANTIC RETRIEVAL (misrouting) (recall problem).

EMERGENCE through HEIRARCHICAL ROUTING.

10 SKILLS exposed to metadata - not entire library.

______________________________________________________________

Antigravity create skill for me.

big why.

skills macro

on demand loading with name and description.

_________________________________________________________________

1st Party Skill

pin versions aggressively

audit code before adoption.

monolith of software fails, because of nuance in data formats.

AGENT SKILLS - data format shifted to DOMAIN EXPERTS.

- PROJECT GUIDANCE SKILL

- data goes in BUS

- triggers materials list SKILL

- delivery window SKILL

- availability SKILL.

- strategic moat: version controlled institutional knowledge.

_____________________________________________________

1) folder SKILL.md

2) PROGRESSIVE_DISCLOSURE defeats ATTENTION_DILUTION and CONTEXT_ROT.

3) TRAJECTORY_SCORING - catches AGENT that stumbles into answers.

4) DAG and file msg bus - deploy - DISTRIBUTED LIBRARY of INSTITUTIONAL KNOWLEDGE.

WHILE AVOIDING a MASSIVE TRAFFIC JAM. Profound Shift.

NOT SMARTER - highly structured and deterministic.

________________________________________________________________________________________________
5-day of AI agents, Vibe coding intensive cours and capstone" by Google, as Google Developer member (gold).
_____________________________________________________________________________________________________



EVALUATION-DRIVEN DEVELOPMENT (EDD) - thre JSON evaluation cases:
1) Input, Tools, Output.
2) FUNCTIONAL SPECIFICATION.

LLM JUDGE, scores OUTPUT.

"remember two non-negotiables: swap the positions of the reference and actual outputs to eliminate ordering bias"

upfront: evaluation rubric, input parameters, and tool trajectroies.

1) CONDITIONAL - triggered only when needed.
2) COMPOSIABLE - one skill can call another. or "chain downstreams".
3) they are owned.


"CONTEXT OVERFLOW"

_______________________________________________

1) humans write the first version of every skill.
2) manage regressions
3) frontier: AGENT PROPOSES SKILLS based on NETWORK TRAFFIC.

EVALUATION GATES - tight - rigid - 

_______________________________________________

avoid circular dependencies.

pass state,
reference tools

> Naive prompt chaining to PRDICTABLE ORCHESTRATION.

DAG ORCHESTRATION:

1) DECOUPLE STATE - for state routing.

__________________________________________________

______________________________________________

The SKILL.md file is the brain of the Skill. It tells the agent what the skill is, when to use it, and how to execute it.

YAML Frontmatter

This is the metadata layer. It is the only part of the skill that is indexed by the agent's high-level router. When a user sends a prompt, the agent semantic-matches the prompt against the description fields of all available skills.


---
name: database-inspector
description: Use this skill when the user asks to query the database, check table schemas, or inspect user data in the local PostgreSQL instance.
---

The Markdown Body

The body contains the instructions. This is "prompt engineering" persisted to a file. When the skill is activated, this content is injected into the agent's context window.

The body should include:

Goal: A clear statement of what the skill achieves.
Instructions: Step-by-step logic.
Examples: Few-shot examples of inputs and outputs to guide the model's performance.
Constraints: "Do not" rules (e.g., "Do not run DELETE queries").
Example SKILL.md Body:

_____________________________________________________________________

Database Inspector

Goal
To safely query the local database and provide insights on the current data state.

Instructions
- Analyze the user's natural language request to understand the data need.
- Formulate a valid SQL query.
 - CRITICAL: Only SELECT statements are allowed.
- Use the script scripts/query_runner.py to execute the SQL.
 - Command: python scripts/query_runner.py "SELECT * FROM..."
- Present the results in a Markdown table.

Constraints
- Never output raw user passwords or API keys.
- If the query returns > 50 rows, summarize the data instead of listing it all.

_______________________________________________________

Script Integration
One of the most powerful features of Skills is the ability to delegate execution to scripts. This allows the agent to perform actions that are difficult or impossible for an LLM to do directly (like binary execution, complex mathematical calculation, or interacting with legacy systems).

Scripts are placed in the scripts/ subdirectory. The SKILL.md references them by relative path.

________________________________________________________

Using Antigravity or Antigravity CLI
Skills can be defined at two scopes, allowing for both project-specific and user-specific i.e. global skills.:

Global Scope (~/.gemini/config/skills/): Available across all Antigravity products (Antigravity, Antigravity IDE, Antigravity CLI) and projects. These skills are available across all projects on the user's machine. This is suitable for general utilities like "Format JSON," "Generate UUIDs," "Review Code Style," or integration with personal productivity tools.
Project/Workspace Scope (<project-root>/.agents/skills/): This would make the skill available only within a specific project. This is ideal for project-specific scripts, such as deployment to a specific environment, database management for that app, or generating boilerplate code for a proprietary framework.


__________________________________________________________

JSON to PYDANTIC

2. **Run the Validation Script**:
  Use the `run_command` tool to execute the python script provided in the `scripts/` folder against the user's file.
 
  `python scripts/validate_schema.py <path_to_user_file>`

____________________________________


"ACTION & LIFECYCLE" Pattern

- scaffolding, boilerplate files, local runtime,.
package in specific agent skills.

_____________________________________

How to Install
Ensure you have Python 3.11+, Node.js, and the uv package manager installed. Then, run the setup command in your terminal:


uvx google-agents-cli setup

_______________________________________

Start the Interactive Web Playground
Launch the built-in, local web-based playground to interact with your agent visually.


agents-cli playground

Project Scope: Located in <project-root>/.agent/skills/.
Global Scope: Located in ~/.gemini/antigravity-cli/skills/.

_____________________________________________________

Vibe Coding:

 natural language becomes the primary programming interface, and learn how to create \"10x agents\" by integrating tools, APIs, and multi-agent systems. 
